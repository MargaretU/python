S = float(input('Введіть значення S = '))
a = float(input('Введіть значення a, на скільки основа більша висоти: '))
if S > a:
    h = float((-a + ((a * a + 8 * S) ** (1 / 2))) / 2)
    hl = (round(h, 2))
    print('Висота = ', hl)
else:
    print('Дані не задовільняють умову')